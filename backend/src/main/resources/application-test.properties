# Test Profile Configuration
spring.profiles.active=test

# Test Database Configuration (H2 in-memory for faster testing)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# H2 Console for debugging (only in test)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration for Testing
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Hub Databases (H2 in-memory for testing)
app.datasource.identity.url=jdbc:h2:mem:identity_testdb;DB_CLOSE_DELAY=-1
app.datasource.identity.username=sa
app.datasource.identity.password=
app.datasource.identity.driver-class-name=org.h2.Driver

app.datasource.user.url=jdbc:h2:mem:user_testdb;DB_CLOSE_DELAY=-1
app.datasource.user.username=sa
app.datasource.user.password=
app.datasource.user.driver-class-name=org.h2.Driver

app.datasource.tenant.url=jdbc:h2:mem:tenant_testdb;DB_CLOSE_DELAY=-1
app.datasource.tenant.username=sa
app.datasource.tenant.password=
app.datasource.tenant.driver-class-name=org.h2.Driver

app.datasource.app.url=jdbc:h2:mem:app_testdb;DB_CLOSE_DELAY=-1
app.datasource.app.username=sa
app.datasource.app.password=
app.datasource.app.driver-class-name=org.h2.Driver

app.datasource.billing.url=jdbc:h2:mem:billing_testdb;DB_CLOSE_DELAY=-1
app.datasource.billing.username=sa
app.datasource.billing.password=
app.datasource.billing.driver-class-name=org.h2.Driver

app.datasource.wallet.url=jdbc:h2:mem:wallet_testdb;DB_CLOSE_DELAY=-1
app.datasource.wallet.username=sa
app.datasource.wallet.password=
app.datasource.wallet.driver-class-name=org.h2.Driver

app.datasource.config.url=jdbc:h2:mem:config_testdb;DB_CLOSE_DELAY=-1
app.datasource.config.username=sa
app.datasource.config.password=
app.datasource.config.driver-class-name=org.h2.Driver

app.datasource.message.url=jdbc:h2:mem:message_testdb;DB_CLOSE_DELAY=-1
app.datasource.message.username=sa
app.datasource.message.password=
app.datasource.message.driver-class-name=org.h2.Driver

# Test-specific configurations
spring.security.jwt.secret=test-jwt-secret-key-for-testing-only
spring.security.jwt.expiration=3600000

# Redis Test Configuration (using embedded Redis if available)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms

# RabbitMQ Test Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Logging Configuration for Testing
logging.level.com.chatbot=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.testcontainers=INFO

# Test-specific application properties
app.name=Chatbot SaaS v2.1 (Test)
app.version=0.1.0-test
app.description=Multi-tenant Chatbot Platform with Hub & Spoke Architecture (Test Environment)

# Disable some integrations for testing
app.integrations.botpress.enabled=false
app.integrations.odoo.enabled=false
app.integrations.minio.enabled=false

# Management endpoints for testing
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
